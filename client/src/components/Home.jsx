import { VIEW_PLAYERS } from '../utils/queries';
import { useQuery } from '@apollo/client';


const Home = () => {
    return (
        <div>
            This is the home page!
        </div>
    )
}

export default Home;